import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  productionContainer: {
    margin: 8,
  },
  kpiCard: {
    paddingBottom: 0,
  },
  kpiRow: {
    paddingBottom: 16,
  },
});

export default styles;
