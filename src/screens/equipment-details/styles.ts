import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  graphContainer: {
    margin: 8,
    marginTop: 16,
  },
  productionContainer: {
    margin: 8,
  },
  kpiCard: {
    paddingBottom: 0,
  },
  kpiRow: {
    paddingBottom: 16,
  },
});

export default styles;
