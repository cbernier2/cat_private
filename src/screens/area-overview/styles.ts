import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  kpiContainer: {
    margin: 8,
  },
  kpiCard: {
    marginBottom: 8,
    paddingBottom: 0,
  },
  kpiRow: {
    marginBottom: 16,
  },
  productionContainer: {
    margin: 16,
    marginTop: 0,
  },
  materialValue: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  materialName: {
    marginLeft: 8,
  },
});
