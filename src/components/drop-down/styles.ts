import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  menuItemTouch: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  menuItem: {
    flex: 1,
  },
});

export default styles;
