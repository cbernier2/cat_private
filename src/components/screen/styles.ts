import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  background: {
    flex: 1,
  },
  triggerContainer: {
    width: 25,
    height: 25,
    position: 'absolute',
    zIndex: 1000,
    right: 0,
  },
});

export default styles;
