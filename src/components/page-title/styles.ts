import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  titleContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    margin: 16,
  },
  titleIcon: {
    marginHorizontal: 16,
  },
  titleText: {
    flexShrink: 1,
  },
});

export default styles;
