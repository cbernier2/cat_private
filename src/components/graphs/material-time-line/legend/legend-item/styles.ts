import {StyleSheet} from 'react-native';

// TODO get real colours
export const styles = StyleSheet.create({
  container: {
    display: 'flex',
    flexDirection: 'row',
    paddingBottom: 10,
  },
  labelText: {
    color: '#555',
  },
  unitText: {
    color: '#fff',
    paddingLeft: 10,
    paddingRight: 15,
  },
});
