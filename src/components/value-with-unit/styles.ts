import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'flex-end',
  },
  valueText: {
    fontWeight: 'bold',
  },
  unitText: {
    marginLeft: 4,
  },
});

export default styles;
